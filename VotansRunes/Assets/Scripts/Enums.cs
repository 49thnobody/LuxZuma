public enum Color
{
    White,
    Yellow,
    Red,
    Green,
    Blue,
    Pink
}

public enum GameState
{
    Menu,
    Play,
    Pause,
    GameOver
}

public enum BallState
{
    Active,
    Moving,
    Road
}