public enum Color
{
    White,
    Yellow,
    LightBlue,
    Red,
    Green,
    Blue
}

public enum GameState
{
    Menu,
    Play,
    Pause,
    GameOver
}

public enum BallState
{
    Active,
    Moving,
    AtRoad
}